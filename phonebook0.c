//implements phone book with out struct

#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    string names = {"EMMA", "RODRIGO", "BRIAN", "DAVID"};
    string nambers = {"617-555-0100", "617-555-0101", "617-555-0102", "617-555-0103"};
    for (int i = 0; i < n; i++)
    {
        if (!strcmp(names[i]), EMMA)
        {
            printf("found %s\n", numbers[i]);
            return 0;
        }
    }
    printf("not found\n");
    return 1;
}