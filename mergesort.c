//mergesort

#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int n = get_int("how much numbers: ")
    int values[n];
    for (int i = 0; i < n; i++)
    {
        values[i] = get_int("Values number %i: ", i + 1)
    }

    
    
}